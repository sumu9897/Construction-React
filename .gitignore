# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# pm-agent
.env
# Transient bot state, not part of the record.
.chase-session-*.json
.agent-state.yaml
# The Project Ledger holds contract sums, rates and claims strategy. Keep it in
# its own private repo via LEDGER_ROOT rather than committing it here.
/projects/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
